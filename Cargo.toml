[package]
name = "SmartRustyPick"
version = "0.1.0"
edition = "2024"

[dependencies]
time = { version = "0.3.47", features = ["local-offset"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
